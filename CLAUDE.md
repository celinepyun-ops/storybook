# Gallop AI — Claude Code Rules

## UI & Design Rules

### Strict Prohibitions
- **No iOS/Unicode emojis** — never use emojis like `...` in any component, badge, button, label, placeholder, or empty state
- When an icon is needed, always use existing SVG icon components from `src/stories/icons.jsx`
- No emojis in text labels, badges, buttons, toast messages, or empty state illustrations
- Only use components and design tokens defined in `DESIGN_SYSTEM.md` and `src/stories/tokens.css`
